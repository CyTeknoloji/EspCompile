name: ESP32 Bulut Derleyici

on:
  workflow_dispatch:
    inputs:
      sketch_code:
        description: 'Arduino/ESP32 Kodu'
        required: true
      libraries:
        description: 'Kütüphaneler (Virgülle ayırın)'
        required: false
      board_type:
        description: 'Kart Modeli (FQBN)'
        required: true
        default: 'esp32:esp32:esp32'

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Arduino CLI Kur
        run: |
          curl -fsSL https://raw.githubusercontent.com/arduino/arduino-cli/master/install.sh | sh
          echo "$GITHUB_WORKSPACE/bin" >> $GITHUB_PATH

      - name: ESP32 Çekirdek Kurulumu
        run: |
          ./bin/arduino-cli core update-index --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
          ./bin/arduino-cli core install esp32:esp32 --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json

      - name: Kütüphaneleri Kur
        run: |
          LIBS="${{ github.event.inputs.libraries }}"
          if [ ! -z "$LIBS" ]; then
            echo "$LIBS" | tr ',' '\n' | while read lib; do
              trim_lib=$(echo "$lib" | xargs)
              [ ! -z "$trim_lib" ] && ./bin/arduino-cli lib install "$trim_lib" || true
            done
          fi

      - name: Sketch Hazırla (Karakter Korumalı)
        shell: python
        env:
          SKETCH_INPUT: ${{ github.event.inputs.sketch_code }}
        run: |
          import os
          content = os.environ.get('SKETCH_INPUT', '')
          os.makedirs('MySketch', exist_ok=True)
          with open('MySketch/MySketch.ino', 'w', encoding='utf-8') as f:
              f.write(content)

      - name: Derle
        run: |
          mkdir -p output
          ./bin/arduino-cli compile --fqbn ${{ github.event.inputs.board_type }} MySketch --output-dir ./output

      - name: Esptool ile Tek Bir 4MB Binary Oluştur
        run: |
          pip install esptool==4.6.2
          cd output
          # Sadece bu dosya kalana kadar diğerlerini sileceğiz ki karışmasın
          python3 -m esptool --chip esp32 merge_bin \
            -o tam_sistem.bin \
            --flash_mode dio \
            --flash_freq 40m \
            --flash_size 4MB \
            0x1000 MySketch.ino.bootloader.bin \
            0x8000 MySketch.ino.partitions.bin \
            0x10000 MySketch.ino.bin
          
          # Gereksiz dosyaları siliyoruz, Android sadece tam_sistem.bin'i görsün
          rm MySketch.ino.bootloader.bin MySketch.ino.partitions.bin MySketch.ino.bin MySketch.ino.merged.bin merged.bin || true

      - name: Binary Dosyalarını Paketle
        run: |
          # ZIP içinde sadece tek bir dosya olacak: tam_sistem.bin
          zip -j esp32_builds.zip output/tam_merged.bin

      - name: Artifact Olarak Yayınla
        uses: actions/upload-artifact@v4
        with:
          name: esp32_artifacts
          path: esp32_builds.zip
