name: ESP32 Bulut Derleyici

on:
  workflow_dispatch:
    inputs:
      sketch_code:
        description: 'Arduino/ESP32 Kodu'
        required: true
      libraries:
        description: 'Kütüphaneler (Virgülle ayırın)'
        required: false
      board_type:
        description: 'Kart Modeli (FQBN)'
        required: true
        default: 'esp32:esp32:esp32'

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Arduino CLI Kur
        run: |
          curl -fsSL https://raw.githubusercontent.com/arduino/arduino-cli/master/install.sh | sh
          echo "$GITHUB_WORKSPACE/bin" >> $GITHUB_PATH

      - name: ESP32 Çekirdek Kurulumu
        run: |
          ./bin/arduino-cli core update-index --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
          ./bin/arduino-cli core install esp32:esp32 --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json

      - name: Kütüphaneleri Kur
        run: |
          LIBS="${{ github.event.inputs.libraries }}"
          if [ ! -z "$LIBS" ]; then
            echo "$LIBS" | tr ',' '\n' | while read lib; do
              trim_lib=$(echo "$lib" | xargs)
              [ ! -z "$trim_lib" ] && ./bin/arduino-cli lib install "$trim_lib" || true
            done
          fi

      - name: Sketch Hazırla
        run: |
          mkdir -p MySketch
          cat << 'EOF' > MySketch/MySketch.ino
          ${{ github.event.inputs.sketch_code }}
          EOF

      - name: Derle ve Parçaları Oluştur
        run: |
          mkdir -p output
          rm -rf output/*
          ./bin/arduino-cli compile --fqbn ${{ github.event.inputs.board_type }} MySketch --output-dir ./output

      - name: Esptool ile Profesyonel Merged Binary Oluştur
        run: |
          pip install esptool
          cd output
          
          # Dosyaları dinamik olarak bul (İsim hatalarını önler)
          BOOTLOADER=$(ls *bootloader.bin | head -n 1)
          PARTITIONS=$(ls *partitions.bin | head -n 1)
          APP="MySketch.ino.bin"
          
          echo "Bulunan Parçalar: $BOOTLOADER, $PARTITIONS, $APP"

          # 4MB Tam İmaj Oluşturma
          # --fill-flash 0xFF sayesinde boşluklar doldurulur ve tam 4MB dosya oluşur
          python3 -m esptool --chip esp32 merge_bin \
            -o final_merged.bin \
            --flash_mode dio \
            --flash_freq 40m \
            --flash_size 4MB \
            0x1000 "$BOOTLOADER" \
            0x8000 "$PARTITIONS" \
            0x10000 "$APP"
          
          echo "Dosya boyutu kontrolü:"
          ls -lh final_merged.bin

      - name: Binary Dosyalarını Paketle
        run: |
          # Sadece 4MB'lık tam dosyayı ve ham uygulama dosyasını gönderiyoruz
          zip -j esp32_builds.zip output/final_merged.bin output/MySketch.ino.bin

      - name: Artifact Olarak Yayınla
        uses: actions/upload-artifact@v4
        with:
          name: esp32_artifacts
          path: esp32_builds.zip
